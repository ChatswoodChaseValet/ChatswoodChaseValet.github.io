# Fix / Change Log — Chatswood Chase Valet App

A running record of everything asked of Claude to fix, add, or investigate in
this repo — so it's easy to recognize "have I already asked for this?" or
"was this already fixed?", and to see at a glance what's still waiting on
real-world confirmation, without digging back through chat history.

**Convention:** one row per request. Add a new row whenever a fix/change is
completed — don't edit old rows except to update **Status**/**Follow-up** if
something changes (a regression, or a pending confirmation coming back
positive/negative). Use **Follow-up** for anything shipped but not yet
provably confirmed in the real app (needs a day of live use, a specific
metric to check, a repeat of the original symptom not recurring, etc.) — put
"—" if the fix was confirmed immediately or needs no further check. Once a
follow-up resolves, update the cell in place (e.g. "✅ confirmed 2026-08-13 —
egress dropped to 8MB/day") and drop it from the Open follow-ups list below.

## 🔭 Open follow-ups

Anything below with an unresolved **Follow-up** cell — check these first when
starting a session, since they're the "did this actually work?" items still
waiting on evidence.

- **"Who's online" presence feature (2026-08-11, commits `2aabc73`/`3ef9023`)**
  — root cause of it not working found and fixed (RLS gap on
  `realtime.messages` + a broken Presence implementation in the pinned
  `supabase-js@2.45.4`, upgraded to `2.112.2`), but final live confirmation
  got blocked by the stale-session issue below before it could be checked
  end-to-end on a real device.
- **Stale session / empty `device_tokens` table (2026-08-11)** — Mick's
  device shows "Cloud returned no entries" because the project's
  `device_tokens` table is completely empty (likely an unrelated historical
  reset), so the cached 24h login can't silently refresh. Needs the user to
  log out and log back in with their PIN to re-mint a working session — then
  re-check the online-staff badge at the same time. **Superseded 2026-09-09**:
  the underlying shared-account mechanism this depended on has been replaced
  (see that date's entry) — a fresh login now mints a per-device identity
  instead, so this specific failure mode shouldn't recur in its old form. Not
  marking fully resolved since the original "who's online" live confirmation
  was never actually completed.
- **Zebra print cold-start, first print of the day (2026-08-22, still
  unresolved)** — confirmed today's silent auto-print failure (Anthony/AG238)
  was genuinely the first check-in of a fresh session, matching the exact
  pattern the 2026-08-09 and 2026-08-16 (`0b065be`) fixes targeted — yet it
  still failed silently (no red error toast, though the code should always
  show one). A later, non-first check-in printed immediately, so the retry
  sweep itself isn't broken — only that very first attempt is unexplained.
  Next occurrence: note the exact time, wait ~90s to see if it self-heals,
  and if possible have DevTools Console open to catch the real error.
- Confirm a normal control-PC refresh/re-login no longer reprints every car
  already on site (regression from the first version of the 2026-08-16 fix,
  caught and patched same day — not yet re-confirmed since).
- **App not used to log check-ins, 17–21 Aug (2026-08-22)** — the DESIGNA
  booking export shows 29 real walk-in valet visits across those 5 days, but
  the app's `entries` table had only 1 record for the same window before
  today's backfill. Needs the user to find out from staff why check-ins
  weren't being logged live in the app that week (device issue? staff
  reverted to a paper/verbal process? one shift only?) — the backfill fixes
  the historical record but not the root cause, so the gap could recur.
- **Customer-field fallback, remaining call sites (`firstNonBlank`,
  2026-08-22, `index_v2.html`)** — New Entry autofill confirmed working
  2026-09-12. Still needs a live check: (a) open "Edit customer" for AG238
  or FJJ50L and confirm the form isn't blank before Save; (b) trigger the
  welcome-back popup for a returning customer with partial data on their
  latest visit.
- **Staff Log (2026-09-09, `index_v2.html`)** — new Reports → Staff Log tab
  reading `audit_log` live. Needs a live check on a real login: open Reports
  → Staff Log, confirm rows actually load (not blocked by RLS/auth in
  practice, only checked via `pg_policies`) and the per-staff filter narrows
  correctly.
- **Unsaved New Entry warning (2026-09-09, `index_v2.html`)** — two layers now:
  (1) `beforeunload` for closing the tab/browser normally — needs a real
  phone check, type into New Entry without saving, close the tab, confirm a
  "leave site?" prompt appears; (2) a separate Android back-button guard
  (`armBackGuard`/`disarmBackGuard`/`popstate`) added after the user reported
  `beforeunload` doesn't fire for the hardware/gesture BACK button on
  Android — needs a live check on their Android phone: type into New Entry,
  press back (expect: stays put, confirm dialog), Cancel then back again
  (expect: dialog again), Discard then back (expect: actually exits). Known
  limit either way: can't catch a hard OS-level app kill (task-switcher
  swipe-away), only normal tab/browser close or in-app back navigation.
  **Update:** confirmed working on Android by the user.
- **New Entry draft autosave (2026-09-09, `index_v2.html`)** — added as a
  device/OS-agnostic backstop after the beforeunload/back-guard layers
  turned out to have iOS gaps with no iPhone available to verify. Core
  save-and-restore round trip confirmed live on desktop (real reload, real
  beforeunload dialog, draft banner + fields restored correctly, Discard
  cleans up) after two bugs found and fixed along the way — the interval
  wiping drafts outside New Entry, then `restoreNewEntryDraftIfAny` deleting
  what it had just restored during boot pre-render. Still needs: a real
  phone check (Android and, whenever possible, iPhone) that a genuine app
  close/reopen — not just a desktop reload — restores the draft; and a check
  that "Check in" from a driveway arrival asks before discarding an existing
  unsaved draft (added but never actually exercised).
- **Staff Coverage chart, live-data check (2026-09-09, `index_v2.html`)** —
  code confirmed deployed and correct, but every automated test reload hit
  `hasSession: false` (device silently dropped to anon — the known
  2026-08-01 shared-account JWT rotation issue, see [[reference_session_heal]]),
  so the chart's actual rendering against real numbers is unverified. Needs
  the user to open Reports → Staff Log themselves on a normally-logged-in
  device and confirm the Gantt timeline + hourly overlap bars look right.
- **Shared-login session drop — root-cause fix (2026-09-09, `staff-auth` v5)**
  — every device now mints its own dedicated Supabase Auth identity instead
  of sharing one account, verified end-to-end with a synthetic test token
  (not a real staff login). Needs real-world confirmation: watch for whether
  "device silently stopped syncing" reports actually stop happening across
  multiple staff/devices over the coming days/weeks.
- **PBKDF2 password-hashing upgrade (2026-09-09, `staff-auth` edge function)**
  — written and reviewed, deployment held pending the user's go-ahead ("not
  yet"). Once deployed: no action needed from staff (existing accounts
  upgrade silently on next login), but worth a login sanity-check same day.
- **Staff table writes still admin-UI-gated only, not DB-enforced
  (2026-09-09)** — any logged-in staff device (not just admins) can still
  INSERT/UPDATE the `staff` table directly (DELETE/TRUNCATE were closed off,
  but not INSERT/UPDATE). Fixing properly means routing Add/Edit Staff
  through the `staff-auth` edge function's existing admin-reauth actions
  (`create`/`update`/`deactivate`), which needs a UI change (re-prompt for
  the admin's own password on each staff edit) — deliberately not done in
  the 2026-09-09 lockdown pass to avoid breaking the live admin screen
  same-day. Worth doing as a deliberate follow-up, not an emergency.
| Date | Asked | Issue found | Result / fix | Status | Follow-up | Ref |
|---|---|---|---|---|---|---|
| 2026-09-12 | "If I stay in Active Cars page, when I press refresh I need it to still be in Active Cars page, don't need it back to dashboard" | `showView()` (the tab-switching function) had zero persistence — pure in-memory DOM state, with Dashboard hardcoded as the initially-active `<div class="view active">` in the HTML. So refreshing from ANY tab (Active Cars, Reports, Customers, etc.) always snapped back to Dashboard, not just from Active Cars specifically | `showView(name)` now saves the displayed view name to `localStorage` (`valet_last_view`, per-device, not cloud-synced — same pattern as the existing Mobile/Desktop UI-mode toggle) right after the non-admin Mobile-lock redirect is applied, so only a view that's actually allowed to show gets remembered. `initializeUI()` reads it back and calls `showView(lastView)` after its existing pre-renders, validated against a known-views allowlist first so a stale/corrupted value can't crash on a missing DOM element. Verified inline script still parses | ✅ Done | ✅ confirmed 2026-09-12 — user refreshed while on Active Cars and it stayed on Active Cars instead of jumping to Dashboard | `index_v2.html` (`showView`, `initializeUI`) |
| 2026-08-09 | Check bandwidth/egress since yesterday | Routine check, not a bug | 8 Aug closed at ~204.8MB (down 57% from 7 Aug's 484MB); cycle total 0.72GB / 250GB — healthy | ✅ Done | — | `BANDWIDTH_LOG.md`, commit `24dc6c4` |
| 2026-08-09 | Will 10,000 customers / 100 handsfree-shopping-with-photos per day increase egress? | Full-reload sync is capped at the most recent 2,000 rows, so plain customer/entry count growth doesn't scale egress. But bag/car check-in photos were stored as base64 text directly inside each row (84–276KB vs ~1.1KB normal) — at ~100 photo jobs/day, that 2,000-row window would fill with heavy rows within ~3 weeks, turning routine refreshes into ~300-400MB pulls | Created `entry-photos` Supabase Storage bucket (public, unguessable paths). Photo capture (bag pickups + car check-in) now uploads there and stores just a short link in the row instead of the full photo. Falls back to the old inline method if offline. | ✅ Done | — | commit `cf10ed3` |
| 2026-08-09 | Fix the report date-range issue too | Reports/CSV/PDF exports only ever read from the local array, which is silently replaced with just the most-recent-2,000-rows on every refresh. Once total entries pass 2,000, a report for an older date range would quietly come back incomplete — no error, just missing data. The "All Time" quick-range button had the same problem picking its start date. | Added `entriesForRange()` — reports/exports now check if the requested date range is already fully loaded; if not, they fetch the exact missing range straight from Supabase before building the report. Live desk views (dashboard, Full Log table) untouched — still instant. | ✅ Done | — | commit `e71f52e` |
| 2026-08-09 | Show today's fixes as a PDF, plain language | N/A — deliverable request | Created `What-We-Fixed-Today.pdf`: plain-English writeup of the two fixes above (problem / risk if unfixed / how it was fixed) | ✅ Done | — | `What-We-Fixed-Today.pdf`, commit `f4567b8` |
| 2026-08-09 | Keep one file recording every fix/add request, with date/issue/result, so it's easy to recognize repeat asks | N/A — process request | Created this file (`FIX_LOG.md`) | ✅ Done | — | this file |
| 2026-08-09 | Made a plain-English PDF version of this log | N/A — deliverable request | Created `Fix-Log.pdf` — same content as this file, written in plain English, regenerated whenever new rows are added | ✅ Done | — | `Fix-Log.pdf`, commit `ca71390` |
| 2026-08-09 | First customer of the day, key tag didn't auto-print — had to press the manual print button, "printer seems cold" | Initial diagnosis assumed the BIXOLON print bridge (WebPrintSDK, `127.0.0.1:18080`) — a fresh page load hasn't reached the local print agent yet, so Chrome's Local Network Access permission is un-primed and the very first auto-print request can silently fail | Added a matching warm-up ping for BIXOLON (`_warmBixolonConn`). **⚠️ Correction: this device actually uses the Zebra printer, not BIXOLON — see next row for the real fix.** Keeping this one too, harmless if BIXOLON is ever used on another device | ⚠️ Misdiagnosed, see next row | — | commit `34192f5` |
| 2026-08-09 | Correction: the printer that failed was Zebra, not BIXOLON | The Zebra path already had a cold-start warm-up fix from 2026-07-20 in place, and it still didn't prevent today's failure. Confirmed with the user: Browser Print app was already open, and the manual press worked on the first try, no retries needed — a pattern that means the very first network request of the session hit a one-off timing race, not the printer/agent genuinely being unavailable | Made the print itself self-heal instead of relying on warm-up timing: `zebraPrint()` now automatically retries once (~1.2s later) on a connect/timeout failure before showing staff an error — only a second failure is treated as real | ✅ Done | ⏳ Watching — confirm no repeat cold-start failure on a future first-print-of-the-day | commit `91933be` |
| 2026-08-09 | Write up the whole process for when a shopping centre wants to buy/use the app, for the user and their business partner | N/A — deliverable request. Grounded in what already exists in the repo (`CLONE-NEW-CENTRE.md` technical checklist, `deck-staff-training.md`, `deck-management.md`, the pitch materials) rather than inventing steps | Created `New-Centre-Playbook.pdf` / `.html`: 8 phases from first sales conversation through commercial terms, technical setup, hardware, staff training, pre-launch testing, go-live, and ongoing support — including the operational note that a fix for one centre must be manually copied to every other centre since each runs a fully separate copy | ✅ Done | — | `New-Centre-Playbook.pdf`, this commit |
| 2026-08-09 | Keep tracking how much SMS cost per day — prompted by a low ClickSend balance warning | Found the real cause of the low balance ($4.93, ~9 days of headroom at the recent burn rate) AND a real bug along the way: the `clicksend-rate` Edge Function's balance/history check was silently stuck returning the account's very first messages from June, no matter when it was called — ClickSend's history API paginates oldest-first with no sort option, and the function never read past page 1 | Fixed the function to walk backward from the last page so "recent" is actually recent, and added a `{"action":"daily"}` mode that gives a real day-by-day cost breakdown. Created `SMS_LOG.md` (same pattern as `BANDWIDTH_LOG.md`) seeded with the last 6 weeks of spend; "check SMS cost" is now a standing trigger that updates it | ✅ Done | ✅ confirmed 2026-08-15 — live balance check's most recent message timestamp was 14 Aug 22:45 UTC (today, not stale June data) | `SMS_LOG.md`, Edge Function `clicksend-rate` v4 |
| 2026-08-09 | Staff can put 2 cars in the same bay number | New Entry and Edit Car both already hard-block assigning a bay another active car holds (`bayOccupant()` check) — but the "Quick Assign Bay" prompt (the button staff use from the dashboard banner to give a bay to a car that came in without one) set the bay directly with no duplicate check at all, so it was the one path that let two active cars land on the same bay | Added the same `bayOccupant()` check to `assignBay()` — it now blocks with the same "Bay X is already taken by …" alert as the other two entry points | ✅ Done | — | commit `df128f3` |
| 2026-08-11 | Keep tracking Supabase egress — why did it climb the last few days, and can this app fit back on the FREE tier (5GB/mo) instead of the $25/mo Pro plan | 9 Aug closed at 384.9MB (99.9% PostgREST) vs 8 Aug's 205MB — no code regression found for that specific day, but digging into the day-to-day *baseline* (60-484MB/day even on "normal" days) found the real structural cost: `reconcileFromCloud`'s "full reconcile" safety net (tab focus/visibility regaining focus, realtime reconnect) was still doing a real `select('*')` full-table pull every time it fired outside the 60s cooldown — cheap per row (~4MB table) but these triggers fire often (staff phones locking/unlocking, tabs refocusing), so it was the actual multiplier behind the daily total, not any single bug. At the current baseline this app would blow the 5GB free cap most months. | Added `lightFullReconcile()`: the safety net now reuses the existing cheap delta (new/changed rows) and closes the one gap a delta can't see — deletes — with a lean `select('id')`-only diff, plus a tiny settings/staff refresh, instead of re-downloading every column of every row. Cuts each "full reconcile" from ~4MB+ down to tens of KB (~80-100x, verified live against production: 2.09MB → 28.5KB). First-ever load and the empty-board self-heal still use a real full pull — correctness there matters more than the byte cost. | ✅ Done | ✅ confirmed 2026-08-13 — 12 Aug closed at 16.52MB, the best day of the cycle by a wide margin (vs 60-484MB/day pre-fix baseline); see `BANDWIDTH_LOG.md` 2026-08-13 entry. Free-tier (5GB/mo) downgrade decision is now unblocked but not made — that's a billing call for the user | `BANDWIDTH_LOG.md`, commit `1783b9b` |
| 2026-08-13 | Check Supabase bandwidth | N/A — routine check | Per-project (`chatswood-valet`) dashboard: **12 Aug closed at 16.52MB** (16.32MB PostgREST, 141.5KB Functions, 53.9KB Auth, 5.8KB Realtime) — resolves the open follow-up below. 13 Aug partial (still today): ~4.67MB so far, same healthy pace. Cumulative this cycle (06 Aug–13 Aug): 1.439GB / 250GB (0.58%) | ✅ Done | — | `BANDWIDTH_LOG.md` |
| 2026-08-11 | How do I make a skill so this log keeps itself updated whenever I ask for a fix/feature | N/A — tooling request | Created `.claude/skills/fix-log/SKILL.md`: a repo-checked-in skill that triggers on any fix/add/investigate request, appends a row here, and pushes to both remotes — formalizes what was previously just a memory-based convention | ✅ Done | — | `.claude/skills/fix-log/SKILL.md`, commit `7d0b324` |
| 2026-08-11 | What do I need to add to the fix-log skill so I can follow up on what I did, what I fixed, when, etc. | N/A — feature add. The existing table (Date/Asked/Result/Status/Ref) captured *what* and *when* but had no way to flag "shipped but not yet confirmed working" | Added a **Follow-up** column to this table plus the "🔭 Open follow-ups" section above — surfaces anything still waiting on real-world confirmation. Updated the skill to fill both in going forward and to check the Open follow-ups list at the start of relevant sessions. | ✅ Done | — | this commit |
| 2026-08-11 | 97.36MB today still feels like too much, especially since no staff used the app today | Queried Supabase's raw request logs directly: the old, expensive `select('*')` full-table query is still firing every 1-3 minutes, all returning 200 OK, as recently as 1:22pm — hours after the reconcile fix deployed. Root cause: GitHub Pages doesn't push code updates into a browser tab that's already open, so any device (desk computer, print station, a phone) that had the app open before the fix went live is still silently running the old leaky code, with zero staff interaction needed to trigger it | Confirmed this is a "stale open tab" problem, not a bug in the fix itself — the fix was independently verified correct earlier the same day. Asked the user to reload every currently-open device to pick up the new code; also corrected an earlier overstated claim (the "72x smaller" figure compared decompressed sizes, not real compressed wire bytes). User also found one device on the wrong URL (`chadmik71.github.io/Valet/`, the old fallback repo, not the canonical `chatswoodchasevalet.github.io`) — confirmed via `git log` + direct `curl` against both live domains that both repos are in sync and both serve today's code, so the fix was never missing from either, purely a caching issue on that tab | ✅ Confirmed fixed | ✅ confirmed 2026-08-11 ~2:00pm — after the user's hard refresh, checked every distinct request in the following 2 minutes of live traffic and found zero instances of the old full `select=*&order=time_in.desc&limit=2000` pull; only the new lightweight delta/id-diff/settings pattern | `BANDWIDTH_LOG.md` |
| 2026-08-11 | Can I have a feature showing how many staff are online at the same time | N/A — feature add, and happened to double as a tool for the egress investigation above (spotting a device that's been open too long) | Added a "who's online" header badge using Supabase Realtime Presence on the existing `valet-changes` channel — every device tracks `{username, displayName, since}` on connect. Badge shows a distinct-staff count (dedup by username, so one person on two devices counts once); click to expand a list of every connected device with how long that browser tab has actually been open | ✅ Done | ⏳ Watching — needs a live check with 2+ real devices to confirm the count and dropdown actually work | commit `2aabc73` |
| 2026-08-11 | Only admin level can see [the online-staff badge] | N/A — access-control follow-up on the feature above; regular staff shouldn't see who else is logged in | Gated the badge to `currentUser.role === 'admin'` before rendering. Non-admin devices still call `.track()` so admins get an accurate count/list regardless of who's connected — this only restricts who's allowed to *see* the resulting badge, not who's counted | ✅ Done | — | commit `59f68c5` |
| 2026-08-11 | I still can't see how many staff online | Isolated testing (fresh test channels, bypassing the app entirely) proved Supabase Realtime Presence never syncs on this project's pinned client library — `.track()` gets accepted by the server ('ok') but the sync/state broadcast never arrives, regardless of RLS or channel config. Along the way also found `realtime.messages` had RLS enabled with **zero policies**, silently blocking Broadcast too — a separate, real, pre-existing bug that was likely also breaking the print-request/print-ack phone-to-print-station handoff | Added an RLS policy granting authenticated staff sessions access to `realtime.messages` (confirmed this fixed Broadcast). Presence itself still failed even after that — traced to the pinned `supabase-js@2.45.4` specifically, confirmed by loading the latest version side-by-side against the same project and watching it work correctly. Upgraded the app's pinned CDN version to `2.112.2` after verifying DB reads, Edge Function calls, Storage URLs, and postgres_changes subscriptions all still work unchanged | ✅ Done (code) | ⏳ Watching — final live confirmation on a real device got blocked by the unrelated stale-session issue below before it could be checked end-to-end | commit `3ef9023`, migration `enable_realtime_authorization_for_valet_changes` |
| 2026-08-11 | (surfaced while debugging the above) | Mick's device showed "Cloud returned no entries — keeping local board" after a hard refresh. Traced to: the `device_tokens` table (used to silently refresh a dead session without a password prompt) is **completely empty** project-wide right now — likely an unrelated historical reset — so this device's cached 24h login looks logged-in locally but can't actually refresh a live Supabase session, and every cloud read silently comes back empty under RLS. Reproduced the same failure via a raw `fetch` bypassing the app entirely, confirming it's unrelated to today's library upgrade | Diagnosed only, not yet fixed — the fix is for the user to log out and log back in with their PIN, which re-mints both a fresh Supabase session and a fresh `device_tokens` row via the normal login path | ⚠️ Diagnosed, action pending | ⏳ Watching — needs the user to log out/back in on the affected device(s); also blocks final confirmation of the row above | — |
| 2026-08-11 | Check Supabase egress/consumption again | N/A — routine check | 11 Aug at 196.9MB so far (99.9% PostgREST), cumulative cycle 1.403GB/250GB (<1%) — healthy. The ~100MB jump since the prior same-day check is attributed to today's own live debugging (repeated manual full-table fetches while isolating the presence bug above), not a regression | ✅ Done | — | `BANDWIDTH_LOG.md` |
| 2026-08-12 | Investigate the recurring `Warp server error: Thread killed by timeout manager` PostgREST log line first flagged on 11 Aug (frequency looked like it was climbing) | Confirmed the message is still firing (34x in the last hour alone, ~every 1-2 min) but cross-checked it against the actual API Gateway request log for the full last 24 hours filtered to error/warning status codes — zero 5xx anywhere, and the only 4xx hits (16 total) are all the already-known 11 Aug stale-session bug (`device_tokens` 403s, `auth/token` 400s), nothing new or ongoing. The Warp log line itself carries no request path/method/status — unlike every real API log entry — meaning it's PostgREST's internal Warp HTTP server recycling an idle worker thread on its own timeout, not a failed client request | No fix needed — confirmed cosmetic/internal, not customer- or egress-impacting | ✅ Done | — | live Supabase logs check |
| 2026-08-13 | Create a README covering all app features with screenshots, "like the manual" | N/A — deliverable request | Wrote `README.md` (was empty) as a repo-landing feature tour: overview, check-in, quick pickup, queue, active cars/checkout, bags, car wash, dashboard, customers, reports, settings, customer ticket page, other integrations (plate OCR, BIXOLON printing, ClickSend SMS, Square Terminal, photo storage), and tech stack — each section illustrated with an existing screenshot from `screenshots/` and `screenshots/manual/` (no new screenshots captured; reused the same images already embedded in `MANUAL.md`, some dated mid-June so newer UI tweaks may not be pictured). Links out to `MANUAL.md` for full staff SOP detail | ✅ Done | — | `README.md` |
| 2026-08-13 | Same feature guide as a PDF with screenshots, to share with their boss | N/A — deliverable request | Built `Valet-App-Feature-Guide.html` (same branded style/CSS as `manual.html`, same content as the new `README.md`) and rendered it to `Valet-App-Feature-Guide.pdf` via headless Chrome (`--headless --print-to-pdf`) — had to render into the scratch dir first and copy in, since headless Chrome couldn't write directly into the repo folder (access denied). 4.1MB output, one page per feature section with screenshots embedded | ✅ Done | ✅ confirmed 2026-08-13 — see follow-up row below (first version was actually broken: 74 pages) | `Valet-App-Feature-Guide.pdf`, `Valet-App-Feature-Guide.html` |
| 2026-08-13 | User asked to open the PDF and check it looks right | The first version was badly broken — 74 pages instead of ~15, caused by two Chromium print-to-pdf quirks: the two-column table-of-contents CSS (`column-count:2` uncapped in print) and full-resolution screenshots (up to 2880×2302px) combined with `break-inside:avoid`, which blew out page height on several sections. User also asked to add a contents list | Capped `.toc{column-count:1}` in print media, added `figure.shot img{max-height:420px;object-fit:contain}` in print media so no image can overflow a page, and added an explicit "Contents" heading above the existing TOC block. Regenerated the PDF (17 pages now) and verified structurally (parsed the PDF: all 17 page content streams are 5.7–22KB decompressed, none blank; 29 image XObjects present) plus visually via the HTML source rendering correctly in-browser (Chrome's built-in PDF-viewer plugin wouldn't screenshot reliably through the browser extension, kept timing out/blank — verified the underlying HTML instead, which shares the same content/CSS) | ✅ Done | — | `Valet-App-Feature-Guide.pdf`, `Valet-App-Feature-Guide.html` |
| 2026-08-13 | Redo README.md to explain how the app was built and what's used to access it | N/A — deliverable request | Replaced the short "Tech stack" bullet list with two fuller sections: **How it was built** (single-file no-build-step frontend, Supabase backend detail — Postgres/Auth/RLS/Realtime/Storage/Edge Functions, ClickSend/Square/BIXOLON integrations, dual-repo GitHub Pages hosting, and how the app is actually developed — Claude Code editing `index_v2.html` directly, git push straight to both live sites, Supabase dashboard for backend changes) and **How to access everything** (a table: live URLs, where admin features live, where the codebase/backend/integration configs are, link to `MANUAL.md`). PDF feature guide (`Valet-App-Feature-Guide.pdf`) was not regenerated this round — only README.md was in scope | ✅ Done | — | `README.md` |
| 2026-08-13 | Redo again: add a documentation hierarchy and document what's excluded from git for privacy | While auditing the README's own image references to write the gitignore section, found a real bug: 4 of the README's screenshots (`00-login.png`, `08-customers.png`, `10-settings.png`, `11-customer-ticket.png`) live under the gitignored `screenshots/` root and were never force-added, so those images were broken on GitHub. Opened all 4 to check for PII before fixing: `00-login`, `08-customers`, `11-customer-ticket` use consistent seeded demo data (fake names, $0 lifetime spend, everything "today") and are safe to publish; `10-settings.png` shows **real staff usernames and login timestamps** (`@harri`, `@kowit`, `@admin`, `@tie`, `@zuu`) and must not be public | Added **Documentation map** (table of every doc in the repo, audience + purpose) and **What's excluded from git (and why)** (each `.gitignore` pattern explained, plus the force-add nuance for `screenshots/manual/*`). Force-added the 3 verified-safe screenshots (user confirmed) so those README images now render; dropped the Settings section's image entirely (replaced with a note explaining why) instead of publishing the unsafe one. Also renamed `README.md` → `Valet_Readme.md` per user request — **note:** this means GitHub will no longer auto-render it as the repo homepage on either `Chadmik71/Valet` or `ChatswoodChaseValet/ChatswoodChaseValet.github.io`, since GitHub only special-cases a file literally named `README.md` | ✅ Done | — | `Valet_Readme.md`, `screenshots/00-login.png`, `screenshots/08-customers.png`, `screenshots/11-customer-ticket.png` |
| 2026-08-15 | Update the customer database from `Valet update.csv` (a booking-export file dropped in Downloads) | N/A — data-import request. Checked first: none of the 13 rows' rego/date combos existed in `entries` yet, so this was genuinely new data, not a re-export of what the app already had | Confirmed with user this should land as full historical visits (not just profile-only records), then inserted 13 backfilled rows into `entries` covering 7–14 Aug 2026: Customer→name, Vehicle Reg→rego, Make→brand, Colour→colour, Mobile Number→phone (normalized `+61`→`0`), Expected Arrival→time_in, Checked Out→time_out (Sydney local converted to UTC). "Kowit Tuntrakool" mapped to attendant_out `KOWIT` (matches existing staff account); "Burin Sengriab" isn't a Valet app staff account so kept verbatim. No fee/payment fields were in the source file, so `parking_fee`/`valet_fee`/`payment_status` were left null rather than guessed (these visits were presumably paid through whatever system produced the export). One anomaly: rego `AYK`'s source "Checked Out" timestamp was 6 days after arrival — used the booking's "Expected Return" time for time_out instead, flagged in that row's notes. All 13 rows tagged `updated_by='csv-import'`, notes carry the original Booking Ref for traceability. Checked every phone number against the known staff-mobile list first — all 13 were genuine customer numbers | ✅ Done | — | Supabase `entries` table (13 rows, `updated_by='csv-import'`) |
| 2026-08-16 | First car entry of the day again didn't auto-print the key tag (recurrence of the 2026-08-09 issue despite commit `91933be`'s single retry) | Traced the real gap: `zebraPrint()`'s built-in retry only fires once (~1.2s later) and only on a `connect`/`timeout` error — if that retry also loses the cold-start race, `printAndTrack` correctly un-marks the tag as unprinted (so a later attempt COULD retry it), but the only thing that ever re-attempts a print is `reconcileFromCloud`'s changed-only diff loop, which compares a BEFORE/AFTER snapshot of `entries`. For a car checked in locally on the print station itself, this device's own `entries` array already held that exact tag content before AND after the failed print — so it never looked "changed" on any later 60s poll, and the failed tag was never retried again until a staff member noticed and pressed the manual print button | Widened the retry sweep in `reconcileFromCloud`: every reconcile cycle now checks EVERY active, bay-assigned car against `autoPrintedSig` (not just ones this cycle's delta flagged as changed) and re-attempts any that aren't confirmed printed. Pure local iteration, no extra network cost — `maybeAutoPrintTag` already no-ops instantly once a tag is confirmed printed. A failed print (cold-start race or otherwise) now self-heals within one ~60s poll cycle instead of needing staff to notice and press the button | ✅ Done | ⏳ Watching — confirm no repeat of the first-print-of-the-day failure on a subsequent morning | `index_v2.html` (`reconcileFromCloud`) |
| 2026-08-16 | After refreshing the dashboard on the control PC and logging in again, it printed a key tag for every car already in the car park | Regression from the same-day retry-sweep fix above: `autoPrintedSig` is in-memory only and doesn't survive a reload, so it starts empty every session. The new sweep checked every active car against it on the first post-login reconcile, saw all of them as "unprinted" (since the map was empty, not because they actually needed printing), and printed the whole board | Seeded `autoPrintedSig` right after the initial `loadFromCloud()` in `completeLogin()` — every car already active at login time is marked as already-printed baseline, so the sweep only fires for cars that are genuinely new or that fail to print later in that same session (which were never in the map to begin with) | ✅ Done | ⏳ Watching — confirm a normal refresh/re-login no longer mass-reprints, alongside the cold-start watch item above | `index_v2.html` (`completeLogin`) |
| 2026-08-15 | A customer left a 1-star rating with no reason — if a customer doesn't give 5 stars, ask them what happened / what could be improved | N/A — feature add. The rating flow (`ticket.html` ratingSection/wireRating), DB (`submit_rating` RPC, `entries.rating_comment`), and dashboard low-rating alert (index_v2.html ~line 6404) already existed end-to-end — the only gap was that the follow-up comment box was the same generic, easy-to-skip "optional" prompt regardless of score, so a 1-star customer had no extra nudge to say why | Score still saves the instant a star is tapped (1-tap, unchanged). The follow-up box now branches on the score: under 5 stars shows "We're sorry it wasn't perfect — what happened, or what could we improve?", a targeted placeholder, an amber/red-tinted box, and a "Submit feedback" button that stays disabled until they type something — with a small "Skip" link always available so no one's forced to answer. 5 stars keeps the original soft "Anything you'd like to add? (optional)" prompt. No DB/RPC change needed — `submit_rating` already coalesces the comment on a follow-up call without clobbering the score | ✅ Done | ✅ confirmed 2026-09-12 — live end-to-end test via browser automation against a disposable test entry (deleted after): tapping 3★ showed the red-tinted "what happened" prompt with Submit disabled until text was typed, then enabled and saved correctly (`rating=3` + comment landed in `entries`); tapping 5★ showed the soft optional prompt with Send already enabled and no red styling; Skip completed the flow without a comment. Also incidentally confirmed a real quirk: `get_ticket_status`'s ticket-based lookup requires `time_out is null`, so a customer reopening the SMS link *after* closing the tab post-pickup won't resolve — the rating prompt only appears via the live 5s poll on a tab that was already open when the car was collected, not on a fresh reload/relink afterward. Not fixing this now since it wasn't reported as a live problem, just flagging as a known edge case | `ticket.html`, `get_ticket_status` RPC |
| 2026-08-22 | Update the customer database from `All_Bookings_260821_190258.csv` (a DESIGNA booking export dropped in Downloads) to match the app's database | N/A — data-import request, but checking first surfaced a real operational gap: querying `entries` for the CSV's 17–21 Aug date range found only 1 row (Billy/DGQ90U) against the CSV's 29 real walk-in visits — the app was essentially not being used to log check-ins that week. That 1 existing row also had `time_out` stuck at a stale sync timestamp (21 Aug 02:06) instead of its real checkout | Confirmed with user this should land as full historical entries (not just customer profiles), and that the stale Billy row should be corrected. Fixed Billy's `time_out` to the CSV's real 17 Aug 12:10 (Sydney). Inserted the other 28 rows into `entries`: Customer→name (title-cased), Vehicle Reg→rego, Checked In/Checked Out→time_in/time_out (Sydney local converted to UTC via `AT TIME ZONE`), Booking Ref→ticket and `id` (prefixed `designa-`). No fee/payment/brand/model/attendant data in the source file, so those columns were left null rather than guessed. All 28 tagged `created_by='csv-import'`, notes carry the original Booking Ref for traceability. Cross-checked all 28 regos against existing `entries` first — all are pre-existing repeat customers with older visits, confirming these are genuine gap-week visits, not duplicates | ✅ Done | ⏳ Watching — root cause of why check-ins weren't logged live that week is still unconfirmed, see Open follow-ups | Supabase `entries` table (28 new rows + 1 corrected, `created_by='csv-import'`) |
| 2026-08-22 | "Why doesn't Anthony/AG238 have phone and vehicle detail in the database" | Direct side-effect of the previous row's backfill: `buildCustomerList()` (index_v2.html) shows only the customer's MOST RECENT visit's phone/brand/model/colour — it doesn't fall back to older visits. Anthony's newest visit is now the 18 Aug DESIGNA-import row, which had no phone/vehicle data in that source file, so it blanked out data that existed on his 3 earlier Aug visits. Checked all 28 backfilled regos: 10 had a name-matching earlier visit with real phone/brand/colour on file, plus 1 more (FRL27B) where the prior record was under a different name ("Shen" vs the CSV's "Jennie") — confirmed with the user this is the same person before touching it | Copied phone/brand/colour from each customer's most recent PRIOR visit onto their backfilled row, for all 11: AG238/Anthony, 55NERI/Nerina, CLG17A/Gemma, DBF68U/Ian, DGF53N/Richard, DGH58N/Wilson, FMB24M/Amira, FPT11B/Terry, FTM23L/Peter, MOD7/O'Donnell, FRL27B/Jennie (confirmed alias of Shen). The other 17 backfilled rows have no prior visit on file at all for that rego, so nothing was missing to restore — that's expected, not a bug. **Not fixed:** the underlying `buildCustomerList()` behavior (latest-visit-only, no fallback) is still there and could blank out real data again any time a customer's newest visit happens to be missing a field — flagged to the user as a possible follow-up, not yet actioned | ✅ Done | — | Supabase `entries` table (11 rows updated) |
| 2026-08-22 | Fix the buildCustomerList fallback too (follow-up to the row above) | Same "latest-visit-only, no fallback" pattern existed in 3 places, not just the Customers tab: `buildCustomerList()`, the New Entry autofill (`lookupCustomerByRego` — fills name/phone/brand/model/colour when staff type a known rego, plus the welcome banner text), and the camera/ANPR arrival-detection lookup (`localPlateLookup`). Any of the three could show blank fields for a known customer whenever their single most recent visit happened to be missing a field (a backfilled row, or just a visit staff didn't fully capture that time) | Added `firstNonBlank(visits, field)` — walks a customer's visits newest-first and returns the first one that actually has that field set. Swapped all three call sites to use it for name/phone/brand/model/colour instead of reading only the newest visit directly. Verified with `node --check` on the extracted script (no other tooling available to run the app itself). This closes the follow-up left open in the row above | ✅ Done | ✅ confirmed 2026-09-12 — user typed AG238 into New Entry and phone/brand/colour autofilled correctly | `index_v2.html` (`firstNonBlank`, `buildCustomerList`, `lookupCustomerByRego`, `localPlateLookup`) |
| 2026-08-22 | "Do a deep test, I wish not issue at all" — asked to thoroughly test the app, especially the recent customer-fallback fix | Widening the same grep beyond the 3 call sites already fixed found the SAME latest-visit-only pattern in 2 more places: `showWelcomePopup()` (welcome-back popup name/brand/model/colour) and, more seriously, `editCustomerByRego()` — the "Edit customer" form pre-fills from `matches[0]` only, and Save (`saveCustomerEdit`) writes the form's values across **every** visit for that plate. If a customer's latest visit was missing a field (any backfilled/incomplete row), the edit form opened with that field blank, and clicking Save without noticing would have permanently erased real data an older visit had on file — a genuine data-loss risk, not just a display gap. Also independently confirmed the underlying bug is real and pre-existing (not just from the CSV backfill): found a live example, rego FJJ50L/"Noah", where the latest visit (21 Jun) has null phone but an earlier one (29 May) has `0416459540` on file | Applied `firstNonBlank()` to both remaining call sites. Verified: (1) `node --check` on the extracted script — clean; (2) unit-tested `firstNonBlank` directly against the real FJJ50L data pulled from Supabase — confirms it now correctly returns the older visit's phone/brand instead of blank; (3) grepped the whole file for every remaining `last.field`/`recent.field`/`c.field` pattern — the only ones left are either manual-customer records (single record, no visit history to fall back across) or `renderCustomers()` reading the already-aggregated list, both correct as-is; (4) confirmed via grep that no other project HTML file (`ticket.html`, `driveway.html`, etc.) contains this pattern. Did not touch live/staff-authenticated UI flows (New Entry, Edit customer form, printer) — those need a real login and are flagged below for the user to confirm on a real device | ✅ Done | ⏳ Watching — needs a live check on a real login: (a) open "Edit customer" for AG238 or FJJ50L and confirm the form is NOT blank before hitting Save; (b) trigger the welcome-back popup for a returning customer whose latest visit has partial data | `index_v2.html` (`showWelcomePopup`, `showCustomerDetail`, `editCustomerByRego`) |
| 2026-08-22 | "I log in already, I have only 3 cars today why it show 6 cars in active car" | Checked `entries` directly: only 3 rows had `time_out IS NULL` (Sarah/GDI61D, Anthony/AG238, Gouskos/EZI31R — all checked in that morning), matching the user's real count exactly. So the extra 3 shown were a display-only problem on that one device, not bad data. Root cause traced to timing, not a new bug: earlier the same session, several direct SQL `UPDATE`s were run on old, already-checked-out DESIGNA-backfill rows (the phone/brand/colour enrichment two rows up) — Supabase's `trg_entries_updated_at` trigger bumps `updated_at=now()` on any write, so ~11 old, unrelated rows all got a fresh `updated_at` within the same hour the user was actively checking cars in. That's an unusually large/clustered delta-sync batch landing on the device mid-session — plausible trigger for a stale render, though the exact client-side mechanism wasn't isolated (no console access to that device) | Asked the user to hard-refresh that device rather than guess further — confirmed fixed, back to showing 3. No code change needed; the underlying data was never wrong | ✅ Done | ✅ confirmed 2026-08-22 same day — refresh fixed it immediately | — |
| 2026-08-22 | "Check zebra printer again" — follow-up on the silent auto-print failure reported earlier this session | Walked through it step by step live with the user: Settings confirmed correct (Print Station ON, printer type Zebra ZD421); manual Test Print worked fine (rules out Browser Print app / USB / driver / printer power); confirmed the original failing check-in (Anthony/AG238) WAS the very first check-in after opening/logging into the app that day — i.e. this is a genuine recurrence of the exact "first-print-of-the-day cold start" pattern the 2026-08-09 (single retry) and 2026-08-16 (`0b065be`, 60s retry sweep) fixes were meant to close. A fresh live test check-in (not the first of the session) printed immediately, so the app is NOT stuck — whatever's failing is specific to that very first print attempt, and everything after self-heals | No code change made — couldn't reproduce the actual failing moment live (would need browser console access on that device at the exact time of a fresh login's first check-in to see what `zebraPrint`'s error path actually hits, since every failure branch in that function normally shows a red toast, and the user says it's fully silent). This closes part of the 2026-08-16 open follow-up (confirms the retry sweep isn't stuck/broken — later prints work fine) but does NOT close the cold-start half of it — that specific first-attempt failure is still unexplained and evidently still happening | ⚠️ Partially confirmed | ⏳ Watching — next time the FIRST print of a session fails: (a) note the exact time, (b) wait ~90s without touching anything to see if the retry sweep silently catches it, (c) if possible have DevTools (F12) → Console open at that exact moment so the real error can be captured, since it should show a red toast per the code but the user reports total silence | `index_v2.html` (`zebraPrint`, `maybeAutoPrintTag`) |
| 2026-08-22 | "Why the customer like Lucie, and James from 17/08 have no mobile number and car brand?" | Both are DESIGNA CSV-imported rows (`id like 'designa-%'`, `created_by='csv-import'`, part of the 28-row 17–21 Aug backfill logged two rows up) — not an app bug. Queried all 28 DESIGNA-import rows: `rego`/`name` are present on every single one (ANPR-captured), but `phone`/`brand`/`colour` split cleanly in two — present together on 11/28 rows, absent together on the other 17/28 (Lucie and James fall in the 17). `model` is null on all 28 with zero exceptions. The binary all-or-nothing pattern per row points to an upstream DESIGNA export limitation (only some bookings carry contact/vehicle detail; model isn't captured at all), not a conversion bug in `jsToDb`/`dbToJs` | No fix applied — nothing to fix in the app. Confirmed via direct SQL against `entries` that the gap is in the source CSV data itself | ✅ Done — investigated, no fix needed | — | Supabase `entries` table query (`id like 'designa-%'`) |
| 2026-08-22 | "I am quite sure from yesterday Richard with rego DYQUEM have phone number but it not show up in customer database" | Confirmed `DYQUEM` has exactly one visit ever (`designa-L29DIBL`, 21 Aug), with no phone on file and no other visit to fall back to — not a `firstNonBlank` bug, genuinely nothing stored. Checked `audit_log`: zero entries for that record, confirming it was never touched by a live check-in or manual edit, only the CSV import | Explained the DESIGNA export gap (same root cause as the row above) and asked the user to supply the number or wait for a fuller export — no code or data change made yet | ✅ Done — explained, correctly predicted a fuller export would resolve it (see row below) | — | — |
| 2026-08-22 | "Can you use `Checked_Out_260822_124211.csv` to update customer database" | This is DESIGNA's "Checked Out" export (different from the earlier "All Bookings" export) and — unlike that one — has Mobile Number/Make/Colour filled in for nearly every row, confirming the earlier rows' blanks were a real upstream export gap, not a permanent DESIGNA limitation. Diffed all 31 CSV rows against `entries` by `ticket`: 19 rows had genuinely new phone/brand/colour to fill; 2 rows conflicted with data already enriched from a different, unrelated visit (`DGF53N`/Richard: DB had Land Rover/Grey from his real 16 Aug check-in vs this CSV's Range Rover/Beige for this booking; `FRL27B`/Jennie: DB had a phone borrowed from an earlier visit under a different name ("Shen") vs this CSV's own number for this exact booking); 2 CSV rows (`AC8JLV8` Nerina/`55NERI`, `F010YLS` Su/`NCS89B`) had a 14 Aug "Expected Arrival" but a "Checked Out" timestamp 4 days later on 18 Aug, both closed by the same staff member one minute apart — a strong admin bulk-close-of-stale-bookings signature rather than a real visit (same pattern as the `AYK` anomaly from the 15 Aug import) | Asked the user to resolve the 2 conflicts and confirm the 2 questionable visits before touching anything. Decisions: kept `DGF53N` as Land Rover/Grey (trust the live check-in over the CSV); updated `FRL27B`'s phone to this booking's own number — user confirmed the rego is a shared car used by different people, so per-visit phone is correct, not a single "the" customer; skipped inserting both 14 Aug bookings — user confirmed from memory that Nerina's only real recent visits were 21 Jul and 19 Aug, matching the bulk-admin-close read. Applied the 19 confirmed updates directly to `entries` by `ticket`, verified all landed correctly by re-querying, including Richard/`DYQUEM`'s phone (`0456393297`) from the row above | ✅ Done | — | Supabase `entries` table (19 rows updated by `ticket`) |
| 2026-08-22 | "Check other customers for similar mismatches" — follow-up on the DGF53N/FRL27B conflicts found above | Grouped all of `entries` by `rego` looking for regos with more than one distinct brand/colour/phone across visits: 46 regos flagged. Most (~30) were pure formatting noise (`KIA` vs `Kia`, `VW` vs `Volkswagen`) or genuine shared/family cars (different phone+name, consistent brand/colour) — no issue. Of the real conflicts: `02BART` (Bartley) flips brand+colour between visits (Range Rover/Gold vs Land Rover/Silver) — user confirmed this is a personalised plate swapped between two of his own cars, not an error. `ERD79D` had one row where the customer's *name* was literally stored as the rego string `"ERD79D"` (artifact of a `csv-sync 2026-07-10` import) — matched by phone to his other visits and corrected to `Aman`. `WSE37K` had 3 name spellings ("J Slater"/"Sliter"/"Asilter") that are clearly the same person mistyped across 3 different CSV syncs — normalized to `Slater`; left its 3 differing phone numbers untouched since there's no way to tell which (if any) is the typo. `NCT29U` (Hakin) — same phone/name but Mercedes-Benz (4 Jul) vs Land Rover (26 Jul), 3 weeks apart on a standard (non-vanity) plate, so less likely a legitimate two-car swap and more likely one visit has a mistyped rego | Fixed the two unambiguous cases (`ERD79D` name, `WSE37K` name spelling). User decided to leave `NCT29U`'s brand conflict as-is rather than guess which visit is correct | ✅ Done | — | Supabase `entries` table (`csv-RVKVVQQ`, `1779410640000-imp117`, `csv-X8UUGEE`, `csv-O2857Q1` updated) |
| 2026-08-31 | "Update customer database" from `Checked_Out_260831_165234.csv` (DESIGNA Checked Out export dropped in Downloads) | Checked all 17 CSV rows against `entries` by `ticket` and by `rego` for the 24–28 Aug window — zero matches either way, meaning this is another app-not-used-to-log-check-ins gap week, same pattern as the 17–21 Aug one (`234f608`). Unlike that first backfill's source (All_Bookings export, which has real "Checked In"/"Checked Out" columns), this Checked Out export only has "Expected Arrival" (booking time) and "Checked Out" (real timestamp) — no actual arrival capture. One row (`CYEZ0IE`/Bri, `NCU13F`) had Colour = "GARY", not a real colour and never used elsewhere in the DB — an unambiguous typo | Confirmed with the user before touching data: (1) insert as new historical entries, (2) use Expected Arrival for `time_in` since there's no better source, (3) normalize "GARY" → "Grey". Inserted all 17 rows into `entries` (`designa-<ticket>` id, `ticket`=Booking Ref, name/brand/colour title-cased, phone normalized to `0XXXXXXXXX`, `created_by='csv-import'`, notes carrying the original Booking Ref per the established convention). Verified all 17 landed with correct Sydney-local timestamps by re-querying | ✅ Done | — | Supabase `entries` table (17 new rows: `designa-59ZZI1X`, `XQ0HWLS`, `IM5QXHR`, `16LS677`, `EPVZZT8`, `26TKQ1F`, `WX14SHC`, `94IEYT7`, `4E9FH1Z`, `IJ4RBA0`, `XZ9AWD4`, `NG52NXW`, `YW1S73F`, `5VEFJ7H`, `WSAXI93`, `CYEZ0IE`, `WVFUNS8`, `8UVKMHH`) |
| 2026-09-05 | "Update customer database" from `Checked_Out_260905_090829.csv` (DESIGNA Checked Out export dropped in Downloads) | Checked all 28 CSV rows against `entries` by `ticket` and by `rego` for the 31 Aug–4 Sep window — zero ticket matches (only 2 real live check-ins logged in the app for the whole window vs 28 real walk-in visits in the export), another app-not-used-to-log-check-ins gap week, same pattern as the two prior Checked Out imports (2026-08-22, 2026-08-31). Two rows (`OV4D16D`/Mehdi, `CQCLEM5`/Alvares) had a "Checked Out" timestamp ~15.7h after arrival, both landing on the exact same minute (04/09 09:24) closed by the same staff member — the same next-morning bulk-admin-close signature flagged in the 2026-08-22 import | Confirmed with the user before touching data: for the 2 anomalous rows, use each booking's own Expected Return time as `time_out` instead of the bulk-close timestamp. Inserted all 28 rows into `entries` (`designa-<ticket>` id, `ticket`=Booking Ref, name/brand/colour title-cased, phone normalized `+61`→`0XXXXXXXXX`, model left null — not captured in this export, `created_by='csv-import'`, notes carrying the original Booking Ref, with the 2 anomalous rows' notes explaining the Expected Return substitution). Verified all 28 landed with correct Sydney-local timestamps by re-querying | ✅ Done | — | Supabase `entries` table (28 new rows: `designa-784TFXN`, `WPXUBFQ`, `TG1ZAC0`, `CZ9BVGD`, `1A685EP`, `ALC9KMM`, `F09HJQF`, `C82VRV0`, `COWRXYG`, `OLWW4VA`, `KWDFACC`, `82ZE18U`, `PHQH5HY`, `CXKFS4G`, `NZ2UNPE`, `CNNVVMT`, `GLVTCIY`, `JJ72GSA`, `6T8BQBL`, `YQ9HP7M`, `NHC5N2W`, `OV4D16D`, `CQCLEM5`, `YKLPKU3`, `4GH04BZ`, `XGKF9K3`, `7SQ2WSW`, `7FIATR9`) |
| 2026-09-05 | "Check for phone/brand mismatches like last time" — follow-up on the DESIGNA import above | Repeated the 2026-08-22 cross-visit audit: grouped all `entries` by `rego`, flagged 28 regos with >1 distinct brand/colour/phone. Most were noise (case/spelling formatting, or genuine shared/family cars with consistent brand/colour) or already-known/decided cases (`ERD79D`, `WSE37K`, `NCT29U`, `CLG17A`, `02BART`, `FRL27B` — all previously resolved or explicitly left as-is). Found 4 new genuine issues: `DGP35B` colour "Silo" (typo, every other visit for that customer says Silver); `GDA67R` had a row with the customer's name literally stored as the rego string "GDA67R" (same import-artifact pattern as the `ERD79D` fix in August, from `csv-sync 2026-07-26`) — same phone has both "Mary" (2 visits) and "Daily" (1 visit) on file; `NBN55M` colour "9" (junk, not a real colour); `FBE81E` name "Lexus Encore" (looks like a leaked dealer/placeholder label, not a customer name, from `csv-sync 2026-06-26`, no phone on that row to cross-check). Also flagged for awareness only (not fixable): the `DGI83S`/Antoun row just imported carries phone `0413585712`, which is actually "Serena"'s number on file for that same rego, not Antoun's usual `0425339369` — likely a shared car, nothing to correct | Confirmed each of the 4 with the user before touching data: `DGP35B` colour → "Silver"; `GDA67R` name → "Mary"; `NBN55M` colour → null (not guessed as Black); `FBE81E` name → "Kevin". Applied all 4 directly by `id` and verified. Left the ambiguous same-phone colour conflicts (`MB9`, `NCN41K`, `WU01`, `FRS57C`, `CLE58Y`, `ALL65N`, `CJ00MR` etc.) and single-digit-differing phone pairs (`DGM11D`, `FPM63S`, `EGQ77Q`) untouched — same reasoning as the `WSE37K`/`NCT29U` calls in August: no way to tell which value (if any) is the error | ✅ Done | — | Supabase `entries` table (`1782522938667-1egc4p`, `csv-55IQRMH`, `1786756791653-pq2bp9`, `csv-X4BAOTJ` updated) |
| 2026-09-09 | Add a "Yesterday" shortcut to the Reports view (Daily Volume / Management Summary) — user says they check yesterday's report more often than today's | N/A — feature add. The Reports view's From/To date range (`#report-from`/`#report-to`) is a single shared control that every report section below it reads from (Management Summary, Daily Volume, Payments, etc. are all just sections filtered by the same range) — there was already `setReportRange('today' / 7 / 'month' / 'all')` but no single-day-in-the-past shortcut | Added a `yesterday` case to `setReportRange()` that sets both From and To to yesterday's date (returns early since the existing today/7/month/all branches all anchor `to` on today, which is wrong for a single past day), and a new "Yesterday" button in the Reports filter bar between "Today" and "Last 7 Days" | ✅ Done | — | `index_v2.html` (`setReportRange`, Reports filter bar) |
| 2026-09-09 | Add a sub-menu under Staff Performance for a "staff log" — wants to see all activity each staff member does | N/A — feature add. `audit_log` (columns: `user_name`, `action`, `entity_type`, `entity_id`, `details` jsonb, `created_at`) already gets a row on nearly every meaningful action app-wide via `cloudAudit()` — check-in/checkout/edit/delete, bag jobs, pickups, car wash, settings changes, staff management, login (2,884 rows since 2 Jun) — but nothing in the UI ever read it back; it was write-only. Confirmed RLS (`auth_full`, `authenticated` role, `ALL`) already permits staff-logged-in reads, no policy change needed | Added a new "Staff Log" item to the Reports flyout (next to Staff Performance) and a matching `data-repgroup="stafflog"` section with a per-staff filter dropdown. `renderStaffLog()` queries `audit_log` live from Supabase for the Reports view's current From/To range (same Sydney-local date convention as `entriesForRange()`), capped at 1000 rows newest-first; `renderStaffLogTable()` renders When/Staff/Action/Details, with a `STAFFLOG_LABELS` map turning action codes (e.g. `edit_time`) into plain text (e.g. "Edited check-in/out time") and `staffLogDetailText()` extracting the useful bits from each row's jsonb `details` (ticket #, rego, amount, status, etc.). Lazy-loaded only while the tab is open (not on every report date-range click) to avoid unnecessary DB hits; re-queries automatically when the date range changes while the tab is open. Verified with `node --check` on the extracted script | ✅ Done | ⏳ Watching — needs a live check on a real login: open Reports → Staff Log, confirm rows load and the staff filter narrows correctly | `index_v2.html` (Reports flyout, `renderStaffLog`, `renderStaffLogTable`, `STAFFLOG_LABELS`, `staffLogDetailText`) |
| 2026-09-09 | Add a warning popup before staff accidentally quit the app — a staff member closed the app on their phone mid-check-in (before pressing Save) and the typed customer/car details were lost | Real gap, not a bug: an existing `beforeunload` handler only guarded against leaving while a cloud write was still in flight (`entriesSyncQueue`/`isCloudSyncing`) — it never checked for typed-but-unsaved form input. Also confirmed staff (non-admin, phone/Mobile-mode users per `MOBILE_VIEWS`) can't reach the Settings-page Sign Out button at all, so "quit" here means closing the browser tab/app itself, not an in-app action | Added `newEntryHasUnsavedData()` (true when the New Entry view is active and rego/name/phone/brand/model/colour has anything typed) and folded it into the existing `beforeunload` handler, so closing the tab/browser mid-check-in now shows the browser's native "leave site?" confirmation instead of silently discarding the form | ✅ Done | ⏳ Watching — confirm on a real phone: type a rego/name into New Entry (don't press Save), then try to close the tab/app and check the browser shows a confirm prompt. Caveat flagged to the user: this can't catch every "quit" — a hard OS-level kill (e.g. swiping the app away in the task switcher) happens outside the page's JS and no client-side code can intercept it; this only catches closing the tab/browser the normal way | `index_v2.html` (`newEntryHasUnsavedData`, `beforeunload` handler) |
| 2026-09-09 | User tested on Android: pressing the phone's back button closes the app instantly with nothing shown — the `beforeunload` warning from the row above never appears | Confirmed known Android Chrome behavior, not a bug in the fix above: `beforeunload` confirm dialogs are unreliable/suppressed specifically for hardware/gesture BACK-button navigation on Android Chrome (a browser-level restriction, not something app code can override) — it only reliably fires for closing via the tab-close UI. Since this app never uses the URL/history API for its internal view switching, there was also zero in-page history for back to consume, so back always exited on the very first press with no chance to intercept anything | Added a separate back-button-specific guard: the moment New Entry has any unsaved rego/name/phone/brand/model/colour typed in (tracked via an `input` listener), `armBackGuard()` pushes one dummy `history` entry. The first back press now fires `popstate` instead of exiting — caught, and a native `confirm()` ("Leave without saving?") is shown; Cancel re-arms the guard so a second back press is caught too, Discard lets the next back press exit for real. Guard clears itself (`disarmBackGuard()`) inside `resetForm()`, so a successful save, the manual "Clear" button, or opening New Entry fresh all leave no stale guard behind (avoids a false "double back press needed to exit" once the data's gone) | ✅ Done | ⏳ Watching — desktop logic verified live (see next row); still needs the physical Android back-button test on the user's actual phone | `index_v2.html` (`armBackGuard`, `disarmBackGuard`, `popstate` handler, `resetForm`) |
| 2026-09-09 | "Test in desktop for me please" (both `beforeunload` warning and the Android back-button guard, from the two rows above) | N/A — verification request. Couldn't test through a real navigation/back-press or a real `confirm()`/native-unload dialog directly, since triggering an actual blocking browser dialog via browser automation is off-limits (would also risk hanging the automated session) | Logged into the live desktop dashboard as Mick (user was already signed in on that device from earlier in the session), opened New Entry, typed a test rego to fire a real `input` event. Then, via a same-origin injected `<script>` tag (needed because the browser extension's own JS context can't see the page's own top-level functions/variables — bridged results back through a DOM attribute), verified the actual app logic end-to-end without ever showing a real dialog: `newEntryHasUnsavedData()` correctly returns `true` once rego is typed; the real `input`-triggered `armBackGuard()` call left `backGuardArmed = true`; dispatching a synthetic `beforeunload` event shows the handler correctly calls `preventDefault()`; dispatching synthetic `popstate` events with `window.confirm` temporarily overridden (so no real dialog shows) confirms Cancel re-arms the guard (`backGuardArmed` back to `true`) and Discard releases it (`false`), both with the expected confirm message; and calling `resetForm()` correctly disarms the guard and clears the field. All paths behaved exactly as designed | ✅ Done — desktop logic confirmed correct | ⏳ Watching — this proves the CODE is correct, not that Android's real back button UI actually triggers it (that's a physical-device thing no amount of code-level testing can substitute for) — still needs the user to physically test on their Android phone | `index_v2.html` (same functions as above) |
| 2026-09-09 | Android back-button guard confirmed working on the user's phone. Follow-up: "what about iPhone, I don't have one to test" | Real gap acknowledged, not chased with a guess: `beforeunload` is well known to be unreliable/suppressed on iOS Safari (Apple restricts it, especially for tab close/backgrounding), so that layer likely won't fire on iPhone at all. The `popstate`/history-based back guard should extend to iOS's edge-swipe-back gesture (same underlying History API, not something Apple specifically blocks) but this is unconfirmed without a physical device, and if the app is ever added to an iPhone home screen as a standalone PWA there may be no back gesture at all in that mode. Neither mechanism — nor anything else running in page JS, on any phone — can catch a hard kill (task-switcher swipe-away), since that happens outside the page entirely | Rather than guess at iOS-specific behaviour with no device to verify against, added a device/OS-agnostic third layer: New Entry now autosaves a draft to `localStorage` (rego/brand/model/name/phone/colour/bay/notes/shop/shop-cover/wash-service/wash-size, deliberately excluding photos/damage markers to keep it small) on every real `input`/`change` event, plus a 4s safety-net interval to also catch programmatic autofills (ANPR plate-scan, "Check in" from a driveway arrival) that don't fire those events. `prepareNewEntry()` now restores any saved draft after a reset (with a dismissible "Restored unsaved details" banner + "Discard draft" button), and re-arms the back-button guard so the restored data is protected too. Draft clears itself on a successful save, the manual "Clear" button, and — to avoid one flow silently overwriting another staff member's unsaved car — `checkInFromArrival()` now confirms before discarding an existing unsaved draft rather than just stomping it. This works regardless of which close mechanism fails (iOS, hard kill, anything) since it never depends on catching a "close" event — the data's just still there next time New Entry opens. Verified with `node --check` on the extracted script | ✅ Done | ⏳ Watching — needs a live check: type into New Entry (don't save), close the tab/app entirely, reopen and confirm the draft banner appears with the typed details restored; also confirm a normal save leaves no stale banner/draft behind, and that tapping "Check in" from an arrival while a draft is unsaved asks before discarding it | `index_v2.html` (`saveNewEntryDraft`, `restoreNewEntryDraftIfAny`, `newEntryDraftTick`, `prepareNewEntry`, `checkInFromArrival`, draft-restored banner) |
| 2026-09-09 | Live desktop test of the draft-autosave feature above (self-directed, following the same "test in desktop" pattern the user asked for on the earlier fixes) | Live-tested with real typing + a real page reload (not synthetic events this time) and found a genuine bug before the user ever hit it: the reload landed on Dashboard first, and the 4s safety-net interval (`newEntryDraftTick`, meant to catch programmatic autofills) ran unconditionally on EVERY view, not just New Entry — so the instant the page loaded on Dashboard, its "no unsaved data here" branch fired and immediately deleted the just-saved draft, before the user ever got back to New Entry to see it. Would have made the whole feature silently do nothing in the exact scenario it was built for | Added an early-return in `newEntryDraftTick()`: it now only acts (save-or-clear, arm-or-disarm) while `#view-new-entry` actually has the `active` class — navigating to any other view, or a fresh page load landing elsewhere, now leaves an existing draft completely untouched instead of wiping it | ✅ Done | ⏳ Watching — needs the same re-test as the row above, this time confirming the draft survives a real reload landing on Dashboard first | `index_v2.html` (`newEntryDraftTick`) |
| 2026-09-09 | Re-tested the interval fix above; draft was STILL wiped by a real reload landing on Dashboard | Second, different bug in the same feature, same underlying mistake in a second spot: `initializeUI()` (app boot) calls `prepareNewEntry()` unconditionally to pre-render every tab, regardless of which one ends up actually visible — so `restoreNewEntryDraftIfAny()` runs and repopulates the (hidden) New Entry fields from the draft every single boot, then asked `newEntryHasUnsavedData()` whether that counted as "worth keeping" — but that function ALSO gates on New Entry being the currently-*visible* view, which it never is yet at this point in boot. So it always read "no unsaved data" and deleted the draft it had just restored, one line after restoring it | Rewrote `restoreNewEntryDraftIfAny()` to judge "is this worth keeping" from the SAVED draft's own field values directly, not by asking `newEntryHasUnsavedData()` (which was never the right check for this — that function exists to answer "should I block navigation right now," a question tied to view visibility that has nothing to do with whether a draft is worth restoring). Re-verified live end-to-end, third time: typed a test rego into New Entry, confirmed the draft in `localStorage`, did a real page reload (real native `beforeunload` dialog fired and blocked it — not synthetic), waited 5s on the Dashboard landing view, confirmed the draft survived in `localStorage`, navigated to New Entry and this time SAW it: fields repopulated with "DRAFT99" and the "Restored unsaved details…" banner showing; clicked "Discard draft" and confirmed both the field and `localStorage` cleared cleanly | ✅ Done — confirmed live end-to-end | — | `index_v2.html` (`restoreNewEntryDraftIfAny`) |
| 2026-09-09 | In Staff Log, add a bar chart showing which staff are using the app at what times, how many overlap at once, and for how long | N/A — feature add. `audit_log` has no explicit "logged out" event (most staff never sign out — Sign Out lives in the desktop/admin-only Settings view, unreachable from `MOBILE_VIEWS`), so there's no direct "session duration" field to chart — had to approximate | Added a "Staff Coverage" subsection to the Staff Log report, above the existing activity table, computed client-side from the already-loaded `staffLogRows` (no extra query). Per day: each staff member's window is approximated as first-activity → last-activity that day (`computeStaffCoverage`), rendered as a Gantt-style horizontal timeline bar per staff (name · positioned bar · "7:12am – 3:40pm · 8h 28m"); below it, an hourly bar chart (`staffHourlyConcurrency`) counting how many staff windows overlap each hour, answering "how many at once." A "Peak overlap" callout up top surfaces the single busiest hour across the whole selected date range. Respects the existing per-staff filter dropdown and the report's date range. Verified with `node --check` on the extracted script | ✅ Done | ⏳ Watching — needs a live check: open Reports → Staff Log for a day with multiple staff, confirm the Gantt bars' times/durations look right against what's actually known about that shift, and that the hourly chart's overlap counts match reality | `index_v2.html` (`computeStaffCoverage`, `staffHourlyConcurrency`, `renderStaffCoverage`, `renderStaffCoverageDay`) |
| 2026-09-09 | Live-verify the Staff Coverage chart above (self-directed) | Confirmed the deployed code is correct (functions and DOM elements present via a real page load, `node --check` clean) but couldn't see it render against real data: every repeated hard-reload/fresh-tab load during this test session came back with `supa.auth.getSession()` → `hasSession: false` — the device had silently dropped to anon, so both `audit_log` and `entries` queries returned 0 rows despite the UI still showing old cached numbers from localStorage. This is the SAME pre-existing issue already documented from 2026-08-01 (shared-account JWT rotation dropping a device to anon) — not a bug in this feature. Repeatedly hard-reloading during testing may itself have contributed, since the account's refresh token is shared across devices and each reload's re-auth attempt can rotate it out from under another session | Did not chase further — re-authenticating requires entering the Access PIN, which is off-limits for me to do myself. Stopped reloading to avoid compounding the shared-token rotation. Left the feature as code-verified but NOT data-verified | ⚠️ Partial — code confirmed correct, live rendering unconfirmed | ⏳ Watching — needs the user to open Staff Log themselves (logging in fresh if prompted) and confirm the Gantt/hourly-overlap chart actually renders with real numbers; if any other device seems to have silently stopped syncing around 2026-09-09, that's this same session-drop issue, not new — log out and back in on that device per the existing fix | — |
| 2026-09-09 | "Use fable model, do deep research on this web app, give me advice on what to add or worry about" | N/A — research request. Spawned a Fable-model agent to read `FIX_LOG.md`, `CLAUDE.md`, `Valet_Readme.md`, `MANUAL.md`, walk the login/session code, and check live Supabase advisors/RLS. Its top "if you only do ONE thing" call: any staff phone could read every password hash (via the permissive `auth_full` RLS policy on `staff`/`audit_log`) and tamper with the audit trail | Delivered the report to the user, who asked to fix the top item. Investigating firsthand (not just trusting the RLS policy text) found the agent's specific password-hash claim was a FALSE POSITIVE — it read the RLS policy alone and missed that `staff.password_hash` already has a separate, correct column-level GRANT lockdown from an earlier security pass (confirmed live: `authenticated`/`anon` can SELECT `username, display_name, role, is_active, last_login, created_at` on `staff` but NOT `password_hash` — tested directly via `SET ROLE authenticated; select username, password_hash from staff` → `permission denied`). The REAL, verified gaps were different: (1) `audit_log` — `authenticated` (any logged-in staff, not just admins) genuinely had UPDATE/DELETE/TRUNCATE via base grants (`relacl` showed `authenticated=arwdDxtm`), confirmed exploitable; (2) `staff` table — `anon` (i.e. **not even logged in**, just holding the public anon key) had full INSERT/UPDATE/DELETE/TRUNCATE — more severe than the original claim, since anyone could've created their own account with zero authentication; `authenticated` also had unnecessary DELETE/TRUNCATE (no code path ever deletes a staff row — deactivation is a soft `is_active` update). `app_config` (shared login credential) and `device_tokens` were already correctly locked (RLS enabled, zero policies = deny-all to non-service-role) | User approved after reviewing the exact SQL. Applied migration `tighten_staff_audit_log_grants`: `audit_log` — revoked UPDATE/DELETE/TRUNCATE from `authenticated` and everything from `anon`; replaced the single `auth_full` policy with separate `authenticated_insert`/`authenticated_select` policies (append-only + read, matching the only two operations the app ever performs on it). `staff` — revoked INSERT/UPDATE/DELETE/TRUNCATE from `anon` entirely, revoked DELETE/TRUNCATE from `authenticated` (INSERT/UPDATE left in place for `authenticated` — Add/Edit Staff still writes directly from the client, see follow-up below). `app_config`/`device_tokens` — revoked the default wide-open base grants too, as defense-in-depth. Verified post-migration via `information_schema.table_privileges` that grants landed exactly as intended, and confirmed via `get_advisors` that no new issues were introduced (the pre-existing "RLS enabled, no policy" INFO-level findings on `app_config`/`data_backups`/`device_tokens`/two old backup tables are the expected, correct deny-all state, not a problem). Also confirmed via grep across `index_v2.html`/`ticket.html`/`driveway.html` that nothing in the app relies on any of the revoked access, before applying | ✅ Done | — | Supabase migration `tighten_staff_audit_log_grants` (project `ebqiitxiyzzbkgyfypss`) |
| 2026-09-09 | Same session — "stronger password hashing" (the other half of the research report's #1 recommendation) | Server-side login (`staff-auth` edge function) and the client's legacy fallback both hash passwords as `SHA-256(password + one hardcoded shared salt)` — no per-account salt, fast/unsalted-in-practice, crackable if `password_hash` were ever exposed by a future misconfiguration (defense-in-depth gap, not a currently-exploitable one per the row above) | Wrote an updated `staff-auth` edge function: per-account-salted PBKDF2-SHA256 at 600,000 iterations (2023 OWASP minimum) for new accounts/password changes, with a `verifyPassword()` that accepts EITHER the old shared-salt-SHA-256 format or the new `pbkdf2$<iterations>$<salt>$<hash>` format — on a successful legacy-format login, it transparently re-hashes and saves the upgraded format, so every account migrates the next time that person logs in, with no forced resets and no downtime. Also added a constant-time comparison (`timingSafeEqual`) to avoid leaking hash-match progress via timing. File is written and ready but **NOT deployed** — user said "not yet" when asked to confirm the edge-function deploy | ⏸️ Prepared, not deployed | ⏳ Watching — needs the user's go-ahead to deploy; file already reviewed and described to the user in chat | scratchpad `staff-auth-index.ts` (not yet pushed to Supabase) |
| 2026-09-09 | "Fix the 2000-row cap for live views" — follow-up on the earlier explanation of what breaks once total entries pass the local cache limit (currently 1,259 of 2,000) | Two distinct real problems, both explained to the user first: (1) `cloudLoadAllEntries()` only ever loads the newest 2000 rows by `time_in`, so a car stuck "checked in, never checked out" from long enough ago could silently fall out of that window and vanish from Active Cars/Dashboard even though it's still physically on site; (2) every customer-recognition path (`findCustomerByRego`/`lookupCustomerByRego`, Customer Database, welcome-back popup, loyalty visit counting) only ever scans the same capped local `entries` array — an infrequent customer (e.g. visits twice a year) would stop being recognised once enough newer traffic pushed their last visit out of the window: no welcome-back banner, no autofill, wrong visit count/lifetime spend, and a wrong loyalty countdown | (1) `cloudLoadAllEntries()` now fetches the recent-2000 batch AND, separately, every row still `time_out IS NULL` (bounded by parking capacity, not total history) and merges them — an active car can no longer disappear from the board no matter how old its check-in is. Added a new `recentBatchOldestTimeIn` (persisted to `localStorage` under `RECENT_OLDEST_KEY`) so `oldestLocalTimeIn()` — which Reports' `entriesForRange()` fallback relies on to know whether it needs to query the cloud for an older date range — tracks the recent-batch boundary specifically, instead of scanning `entries` directly; without this, one old stray active row pulled in by the fix above would have made `entriesForRange()` wrongly think local data had contiguous coverage all the way back to it, silently under-reporting older report ranges. (2) Added a new Postgres RPC `find_entries_by_rego(p_rego text)` (`SECURITY INVOKER`, so it's still bound by the existing `entries` RLS — no privilege escalation; granted to `authenticated` only) that normalises rego the same way the client does (`upper`, strip spaces/hyphens/underscores) and searches the FULL history, not capped. Refactored `lookupCustomerByRego()`: the existing instant synchronous local check is unchanged for the common case; when it finds nothing, a new token-guarded `lookupCustomerByRegoCloud()` queries the RPC in the background and — if a real match comes back before the staff member has typed something else — applies it through a new shared `applyCustomerMatch()` (extracted from the original function body) exactly as a local hit would: autofill, returning-customer banner, welcome popup, loyalty visit count, all now correct regardless of how long ago the customer last visited | Verified the RPC directly against live data: normalisation confirmed correct (`'nb g-51p'` correctly matched a stored `'NBG51P'` row); confirmed via `SET ROLE anon` that anon gets 0 rows even if function-execute privilege is ambiguous (RLS on `entries` still gates the actual data, since the function is invoker-rights, not definer). `node --check` clean on the extracted client script | ✅ Done | ⏳ Watching — needs a live check: find or create a test entry, confirm it doesn't vanish from Active Cars if artificially aged past the 2000-row mark isn't practical to simulate live, so this is more "trust the logic, watch for it in practice as the count climbs"; more provable now: type a rego with NO local match into New Entry and confirm the returning-customer banner still appears if that plate has any history at all in the cloud | `index_v2.html` (`cloudLoadAllEntries`, `oldestLocalTimeIn`, `lookupCustomerByRego`, `applyCustomerMatch`, `lookupCustomerByRegoCloud`), Supabase RPC `find_entries_by_rego` (migration `add_find_entries_by_rego_rpc`) |
| 2026-09-09 | "Fix the shared-login session drop for good" — root-cause fix for the recurring issue first patched 2026-08-01, still recurring as recently as earlier today | Root cause, confirmed by re-reading the actual mechanism: every device that logged in adopted a session for the SAME single shared Supabase Auth account (`app_config.shared_login`, minted via `mintSession()` in the `staff-auth` edge function). Supabase rotates refresh tokens on every use — so any one device's background token refresh silently invalidated every OTHER device's refresh token, since they were all literally the same `auth.users` row fighting over one refresh-token chain. The 2026-08-01 device-token/`ensureAuthSession()` fix only ever healed this AFTER the fact (detect a dead session, re-mint from the shared account) — it never stopped the collision from happening in the first place, which is why it kept recurring | Replaced the shared account with a dedicated Supabase Auth identity PER DEVICE. New `deviceCreds(deviceToken)`/`mintDeviceSession()` in `staff-auth`: derives a deterministic synthetic email+password from that device's own `device_token` (itself already a random 32-byte secret issued at login — nothing new to store), signs in, and creates the identity on first use only (same proven sign-in-or-create pattern the old code already used, just scoped per device instead of shared). `login` and `refresh` both now call this instead of the old shared `mintSession()` (removed). Two devices can no longer collide — they're different `auth.users` rows with independent refresh-token chains, so one refreshing can never invalidate another's session again. No DB schema change needed (reuses the existing `device_tokens` table as-is); no client-side change needed (`ensureAuthSession()`/`cloudLogin()` were already agnostic to which underlying identity a session belongs to) — updated its comment, which described the now-fixed root cause, to explain the fix and reframe the healing path as a general safety net rather than the primary defense. One known residual case, much narrower than before: if the SAME staff member is simultaneously logged into two devices under their own username, those two (and only those two) still share one identity and could still collide — closing that fully would mean an identity per login-event rather than per longer-lived device, not done here as it wasn't the reported failure mode | Deployed (`staff-auth` v5) and verified end-to-end without needing real staff credentials: seeded a temporary `device_tokens` row, called `action:'refresh'` twice — first call minted a brand-new identity (`device-d8bcde…@chatswood-valet.internal`, confirmed via the returned JWT's `email`/`role:authenticated` claims), second call reused the exact same `auth.users` id with no duplicate creation, proving both the create-once and reuse paths work. Cleaned up the test device_tokens row afterward. Also smoke-tested the unrelated `list` action to confirm the redeployed function boots cleanly. `node --check` clean on the extracted client script (comment-only change there) | ✅ Done | ⏳ Watching — the real confirmation is time: no more "device silently stopped syncing" reports across multiple staff/devices going forward. Also hasn't been exercised by an actual real staff login yet (only the synthetic test token) — worth noting if anything about real login behaves differently than the test path (it shouldn't — same code, same function) | Supabase edge function `staff-auth` v5 (`mintDeviceSession`, `deviceCreds`), `index_v2.html` (`ensureAuthSession` comment only) |
| 2026-09-09 | "When I refresh the dashboard page, why does it always show some widget I don't use for a few seconds then gone" | Traced it exactly: dashboard widget visibility/order (`dash_layout`) lives in `cloudSettings`, which starts as an empty `{}` on every fresh page load and is only populated once the async cloud fetch resolves. But `initializeUI()` calls `renderDashboard()` synchronously BEFORE that fetch finishes (by design, so the dashboard isn't blank while waiting) — at that moment `getDashLayout()` finds no usable `dash_layout`/`dash_layout_v` yet, so it falls back to the full default widget set (`DASH_DEFAULT`, 12 widgets) regardless of what the user actually customized. A moment later the real cloud settings arrive, a re-render applies the user's actual (trimmed) layout, and any widgets they'd removed vanish again — the "flash" the user described | Cached `cloudSettings` to `localStorage` (`cacheCloudSettings()`, written right after both places `cloudSettings` gets reassigned from a fresh cloud fetch) and restored it in `loadData()` — which already runs before `initializeUI()`'s first render whenever a page load starts with an empty `entries` array (i.e. every real refresh). So the very first paint now already has the user's real saved widget layout instead of the full default set; the later cloud fetch just silently reconfirms it (typically a no-op re-render since it usually matches). Guarded behind `!DEMO_MODE`, consistent with how `loadData()` already avoids leaking real cached state into demo mode elsewhere | ✅ Done | ⏳ Watching — needs a live check: refresh the dashboard a few times and confirm the previously-flashing extra widget(s) no longer appear even briefly | `index_v2.html` (`cacheCloudSettings`, `loadData`, both `cloudSettings = freshSettings` call sites) |
| 2026-09-09 | "When you show my name mick in log page, can you show Mick instead of admin" | Staff Log's `audit_log.user_name` is the literal login username, not the friendly name — for Mick that's `admin`, so the Staff Log table, staff filter dropdown, and Staff Coverage chart were all showing "admin" instead of "Mick" | Wired the already-existing `staffDisplayName(username)` helper (used elsewhere for the bag-assign picker, previously not used anywhere in Staff Log) into every place a raw username was shown: the Staff column in the activity table, the filter dropdown's labels (kept the underlying `value` as the raw username so filtering logic is unaffected, sorted the options by display name instead of username), the Staff Coverage Gantt row labels/tooltips, and `staffLogDetailText()`'s `to`/`by` fields (bag reassignment details, etc.) — falls back to the raw value unchanged if it's not actually a staff username (e.g. `plate_alias`'s `to` is a rego, not a person), so nothing else visibly changes. `node --check` clean | ✅ Done | — | `index_v2.html` (`renderStaffLog`, `renderStaffLogTable`, `staffLogDetailText`, `renderStaffCoverageDay`) |
| 2026-09-11 | "Update customer database" from `Checked_Out_260911_175525.csv` (DESIGNA Checked Out export dropped in Downloads) | Checked all 24 CSV rows against `entries` by `ticket` and by `rego` for the 7–11 Sep window — zero ticket matches, another app-not-used-to-log-check-ins gap week, same pattern as the four prior Checked Out imports (2026-08-22, 2026-08-31, 2026-09-05). One row (`WCKX8FW`/Tiffany) had a "Checked Out" timestamp 16h45m after its Expected Return, crossing midnight — same next-day bulk-admin-close signature flagged in those prior imports. One row (`40346W9`, rego `NCD47B`) had garbage source data: name "XXX" and a 9-digit phone `041327444` (missing the trailing digit) — cross-checked against an existing visit on the same rego (ticket `T96TZOR`, 10 Jul) which has the real name "Zena" and full phone `0413274444`, confirming the CSV row is a corrupted export of the same known customer. Two rows (`N17WHVH`/`FZG81P`/Kanj, `JCP25VY`/`QXQ888`/S Matrakul) had blank colour with a same-phone prior visit on file showing a colour | Confirmed with the user before touching data: (1) use Expected Return (07/09 16:45 Sydney) as `time_out` for `WCKX8FW` instead of the bulk-close timestamp; (2) use the known-good name/phone (Zena/`0413274444`) for `40346W9`; (3) leave the two blank colours as `null` rather than backfilling from an older visit (unlike the 2026-08-22 precedent) — user's call this time. Inserted all 24 rows into `entries` (`designa-<ticket>` id, `ticket`=Booking Ref, name/brand/colour title-cased, phone normalized `+61`→`0XXXXXXXXX`, model left null — not captured in this export, `created_by='csv-import'`, notes carrying the original Booking Ref, with `WCKX8FW`'s and `40346W9`'s notes explaining their respective overrides). Verified all 24 landed with correct Sydney-local timestamps by re-querying (`WCKX8FW` confirmed at 06:45 UTC = 16:45 Sydney, matching the Expected Return substitution) | ✅ Done | — | Supabase `entries` table (24 new rows: `designa-WCKX8FW`, `6YU5KQD`, `AUULMZY`, `Z41AMRV`, `9GR4XLK`, `JCP25VY`, `N17WHVH`, `FWKAVTJ`, `4O4GD7N`, `GPMDDQ8`, `ZWVCH03`, `OHB5CE4`, `PCUWGIC`, `HHTQCS5`, `5T1HAJ8`, `VNBGKHF`, `TVPOYBK`, `EMJ8O6W`, `40346W9`, `5YC6JC2`, `IBUMD11`, `HIAG466`, `9WNDMJM`, `6AEFX4F`) |
| 2026-09-11 | "Check for phone/brand mismatches like last time" — follow-up on the CSV import above | Repeated the 2026-08-22/2026-09-05 cross-visit audit: grouped all `entries` by `rego`, flagged 32 regos with >1 distinct brand/colour/phone. Most were pure case/spelling noise or already-known/decided cases (`02BART`, `ERD79D`, `WSE37K`, `NCT29U`, `CLG17A`, `FRL27B`, `GDA67R`, `MB9`/`NCN41K`/`WU01`/`FRS57C`/`CLE58Y`/`ALL65N`/`CJ00MR`, `DGM11D`/`FPM63S`/`EGQ77Q` — all previously resolved or explicitly left ambiguous) or vanity plates plausibly shared by two people (`477`, `71717`, `DM4`, `FSY75M`, `FYV18K`, `LC9119`, `NCK64P` — consistent brand/colour, just two different phones/names, same pattern as `02BART`). Found 3 new genuine outliers: `DGG03X` (I/Irena Otvosi) colour "Grey" on its newest visit (6 Sep) vs "Green" on the other 3; `FBE81E` (Kevin) colour "Gray" on one visit (23 May) vs "Green" on 4 others; `GEH87K` (Justin) colour "Y" (junk, not a real colour) on one visit vs "Black" on the other (today's import). Also noticed `DGP35B` cleanly switches driver from "Greg" to "Joey" (different phone) starting 31 Jul with zero interleaving — read as a legitimate handover, not an error, flagged for awareness only | Confirmed all 3 outlier fixes with the user before touching data. Applied: `DGG03X`/id `1788663008353-k26967` colour → Green; `FBE81E`/id `1779502800000-imp135` colour → Green; `GEH87K`/id `1788568745435-9t7czu` colour → Black. Verified all 3 by re-querying. Left `DGP35B`'s driver split untouched per the "legitimate handover" read. **Correction (same day):** user confirmed `DGG03X` is actually Grey, not Green — the 3-vs-1 majority call was backwards. Reverted all 4 `DGG03X` visits to Grey | ✅ Done, 1 correction applied | — | Supabase `entries` table (`1788663008353-k26967`, `1779502800000-imp135`, `1788568745435-9t7czu` updated; all 4 `DGG03X` rows corrected to Grey) |
| 2026-09-11 | "When i check customer history, i can see only which date day came, can i see what's time day came too" | The "Recent Visits" table in the customer detail popup (`showCustomerDetail`) only rendered `fmtDate(tIn)` in the Date column — check-in time was dropped entirely, even though it's on the record | Added `fmtTime(tIn)` next to the date in that column, muted colour so it reads as secondary to the date | ✅ Done | — | `index_v2.html` (`showCustomerDetail` Recent Visits table) |
| 2026-09-12 | "Check number plate FSF10A, how many times visited" → then "Customer Database shows only today's visit" (persisted after a hard refresh) | Confirmed via direct DB query the plate has 2 real visits (23 May 2026 import + today), but both the Customer Database table and the customer detail popup only showed 1 ("First visit: 12 Sept 2026"). Ruled out RLS (policy is wide-open `true` for `authenticated`), ruled out client-side grouping logic (`buildCustomerList`/`findCustomerByRego` are correct, both just read the global `entries` array), ruled out stale deployment (both GH Pages repos in sync, live file matches current code) and ruled out simple localStorage staleness (hard refresh re-runs the full cloud load and didn't fix it). Root cause: Supabase's project-level PostgREST "Max Rows" setting silently caps every REST response below whatever `.limit()` the client code requests, with zero error surfaced — a single `.limit(2000)` call can come back with far fewer rows and no way to detect it client-side. The 23 May row ranks 1171st out of 1296 by `time_in` descending, right in the truncated-off range. Worse: `lightFullReconcile()` (fires on every tab-focus/reconnect) does an id-only fetch with the same unpaginated `.limit(2000)`, then **deletes** any local entry whose id isn't in that (truncated) set — so even immediately after a correct full load, the next routine reconcile would silently prune the older row straight back out, which is why the hard refresh didn't stick | Rewrote both cloud reads to paginate via `.range()` in 500-row chunks instead of a single `.limit()` call: added `fetchRecentEntries(target)` used by `cloudLoadAllEntries()`, and paginated the id-diff fetch inside `lightFullReconcile()` the same way. Verified all inline `<script>` blocks still parse (`new Function()` check). Pushed to both `origin` and `neworigin` (commit `4d96cad`) | ✅ Done | ✅ confirmed 2026-09-12 — user hard-refreshed the live dashboard and FSF10A/Gina now shows 2 visits (23 May and today) | `index_v2.html` (`fetchRecentEntries`, `cloudLoadAllEntries`, `lightFullReconcile`) |
| 2026-09-12 | "When valet staff log a car in, when they press save the popup shows something like the data will not save, can you check it" | First ruled out real data loss: queried the live `entries` table directly and confirmed all 15 of today's check-ins landed correctly with no gaps, including through a genuine ~1-minute Supabase/Cloudflare edge blip (two `522` gateway timeouts on writes at 3:38-3:39pm) — those two writes self-healed via retry within ~30s. So no car was ever actually lost. The warning itself is `verifyCheckinSynced()`'s post-save sync check — it waits 1.5s, and if the entry is still queued ("stranded"), tries one remediation `doCloudSync()` call then waits only 500ms more before declaring "has NOT synced to the cloud". Found the real bug: `doCloudSync()` silently no-ops if a sync is already in flight (`if (isCloudSyncing) return;`) — so if the *original* save from Save Entry is still legitimately in progress after 1.5s (normal on any slightly slow connection, or during a blip like today's), the remediation call does nothing, and 500ms isn't enough for the original save to finish — producing a false "won't save" alarm on a car that is in fact saving fine seconds later | Changed `verifyCheckinSynced()` to poll every 1s for up to 6s (was a single 1.5s check) before attempting remediation, and extended the post-remediation wait from 500ms to 1.5s — gives the real save realistic time to finish before the code gives up and warns staff. Verified all inline `<script>` blocks still parse. Pushed to both `origin` and `neworigin`. Tested two ways before calling it done: (1) sandboxed the exact shipped function with mocked timing — old code false-alarmed at 2s on a sync that self-healed at 3s, new code correctly waited and showed nothing; a genuinely-stuck sync still warned under the new code too (~7.5s), confirming real failures aren't masked; (2) live end-to-end — user saved a real test entry (`TESTFIX01`→`TEST001`, since deleted) on the actual site, landed on Supabase's first attempt (single clean `POST 201`, no retry needed) with the normal green "Entry Saved" confirmation, no false warning | ✅ Done | ✅ confirmed 2026-09-12 — live test save showed the normal green confirmation with no false alarm; the sandboxed test independently proved the fix tolerates slow syncs without masking genuine failures | `index_v2.html` (`verifyCheckinSynced`) |
| 2026-09-19 | Update the customer database from `Checked_Out_260919_091045.csv` (DESIGNA Checked Out export dropped in Downloads) | N/A — data-import request. Checked all 21 CSV rows against `entries` by `ticket` and by `rego` for the 14–18 Sep window — zero matches, another app-not-used-to-log-check-ins gap week, same pattern as the four prior Checked Out imports (2026-08-22, 2026-08-31, 2026-09-05, 2026-09-11). One row (`KEQXVBW`, name "BEROUKAS", rego "TEST") used David Beroukas's own staff mobile (`0420836664`, matches his staff record) and lasted 1 minute — an internal test booking, not a real customer. Four rows (`80MXRK6`/Racnger/`EIR68H`, `N0GH7XD`/Xu/`EYJ36W`, `JJ64XB5`/Do/`YGW02K`, `N33N50G`/C Black/`ENX16C`) had Expected Arrival dates from 3 Sep and 11 Sep but were all "Checked Out" at the identical minute (16/09 14:40) by Burin Sengriab — the same bulk-admin-close signature flagged in the 2026-08-22, 2026-09-05, and 2026-09-11 imports | Confirmed with the user before touching data: (1) skip the TEST row entirely; (2) use each booking's own Expected Return as `time_out` for the 4 bulk-close rows instead of the 16/09 14:40 timestamp. Inserted the remaining 20 rows into `entries` (`designa-<ticket>` id, `ticket`=Booking Ref, name/brand/colour title-cased, phone normalized `+61`→`0XXXXXXXXX`, model left null — not captured in this export, `created_by='csv-import'`, notes carrying the original Booking Ref, with the 4 bulk-close rows' notes explaining the Expected Return substitution). Verified all 20 landed with correct Sydney-local timestamps by re-querying against the CSV's own Expected Arrival/Return and Checked Out columns | ✅ Done | — | Supabase `entries` table (20 new rows: `designa-K5JPIU6`, `9TJIOLF`, `QMY9YC0`, `DX57660`, `37W2RFK`, `GTX1WWP`, `MGFU7Z3`, `38N6UIO`, `PS464RN`, `80MXRK6`, `N0GH7XD`, `JJ64XB5`, `N33N50G`, `6FIOV26`, `4P4GA74`, `R25B0QO`, `LND45Y2`, `O58HJHA`, `GOP64PV`, `TGD0043`) |
